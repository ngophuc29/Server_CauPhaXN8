import { ApsConfiguration as IApsConfiguration } from './IApsConfiguration';
import { AuthClient as IAuthClient } from './IAuthClient';
import { IResiliencyConfiguration } from './IResiliencyConfiguration';
import { ISdkManager } from './ISdkManager';
import { ApsClient as IApsClient } from './IApsClient';
import { Logger as ILogger } from './ILogger';
export declare class SdkManager implements ISdkManager {
    private _apsConfiguration;
    private _resiliencyConfiguration;
    private _authClient;
    private _apsClient;
    private _logger;
    private constructor();
    set logger(logger: ILogger);
    get logger(): ILogger;
    static getInstance(apsConfiguration: IApsConfiguration, resiliencyConfiguration: IResiliencyConfiguration, authClient: IAuthClient, logger: ILogger): SdkManager;
    get apsClient(): IApsClient;
    get authClient(): IAuthClient;
    get apsConfiguration(): IApsConfiguration;
    set apsConfiguration(apsConfiguration: IApsConfiguration);
}
